$('goalForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),g={};['target_weight_kg','target_waist_cm','target_chest_cm','target_arm_cm','target_thigh_cm','target_body_fat_pct'].forEach(k=>g[k]=f.get(k)===''?null:Number(f.get(k)));g.target_date=String(f.get('target_date')||'')||null;g.weekly_sessions_target=clamp(Number(f.get('weekly_sessions_target'))||5,1,7);msg('goalMsg','Salvando metas…');const online=await queueOrSend('save_goal',{goal:g});S.goals={...(S.goals||{}),...g,user_id:U.id};saveCache();if(online)await refresh(false);msg('goalMsg',online?'Metas salvas na nuvem.':'Metas salvas no aparelho e aguardando sincronização.');renderAll()};
$('cpForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),c={client_ref:uuid()};['measured_at','notes'].forEach(k=>c[k]=String(f.get(k)||''));['weight_kg','waist_cm','chest_cm','arm_cm','thigh_cm','body_fat_pct','sleep_hours','energy'].forEach(k=>c[k]=f.get(k)===''?null:Number(f.get(k)));msg('cpMsg','Salvando…');const online=await queueOrSend('save_checkpoint',{checkpoint:c});if(!online){S.checkpoints.unshift({...c,id:'local-'+c.client_ref})}else await refresh(false);saveCache();msg('cpMsg',online?'Checkpoint salvo na nuvem.':'Checkpoint salvo no aparelho e aguardando sincronização.');e.currentTarget.reset();e.currentTarget.elements.measured_at.value=iso();renderAll()};
function fillGoalForm(){const f=$('goalForm'),g=S.goals||{};Object.keys(goalKeys).forEach(k=>{const n=goalKeys[k];f.elements[n].value=g[n]??''});f.elements.target_date.value=g.target_date||'';f.elements.weekly_sessions_target.value=g.weekly_sessions_target||5;$('goalDateBadge').textContent=g.target_date?`prazo ${fmt(g.target_date)}`:'sem prazo'}
function renderGoalCards(){const keys=Object.keys(bodyLabels);$('goalProgress').innerHTML=keys.map(k=>{const p=goalProgressFor(k),u=unitFor(k);if(p.goal==null)return`<div class="goalcard"><div class="goalhead"><b>${bodyLabels[k]}</b><div class="spacer"></div><span>sem meta</span></div><div class="goalfoot">Defina uma meta acima.</div></div>`;if(p.current==null)return`<div class="goalcard"><div class="goalhead"><b>${bodyLabels[k]}</b><div class="spacer"></div><span>meta ${num(p.goal)} ${u}</span></div><div class="goalfoot">Faça um checkpoint para iniciar o acompanhamento.</div></div>`;return`<div class="goalcard"><div class="goalhead"><b>${bodyLabels[k]}</b><div class="spacer"></div><span>${p.reached?'atingida':'em andamento'}</span></div><div class="goalvals">início <strong>${num(p.base)} ${u}</strong> → atual <strong>${num(p.current)} ${u}</strong> → meta <strong>${num(p.goal)} ${u}</strong></div><div class="progressbar"><i style="width:${p.pct}%"></i></div><div class="goalfoot">${p.reached?'Meta atingida.':`faltam ${num(p.remaining)} ${u} • ${Math.round(p.pct)}% concluído`}</div></div>`}).join('')}
function renderCP(){const c=[...S.checkpoints].sort((a,b)=>a.measured_at.localeCompare(b.measured_at)),first=c[0],last=c.at(-1);$('cpWeight').textContent=last?.weight_kg!=null?`${num(last.weight_kg)} kg`:'—';$('cpWaist').textContent=last?.waist_cm!=null?`${num(last.waist_cm)} cm`:'—';$('cpDelta').textContent=first&&last&&first.weight_kg!=null&&last.weight_kg!=null?`${Number(last.weight_kg)-Number(first.weight_kg)>=0?'+':''}${num(Number(last.weight_kg)-Number(first.weight_kg))} kg`:'—';$('cpCount').textContent=c.length;fillGoalForm();renderGoalCards();$('cpHistory').innerHTML=c.length?[...c].reverse().slice(0,25).map(x=>`<div class="hist"><div class="mut">${fmt(x.measured_at)}</div><div><b>${x.weight_kg!=null?num(x.weight_kg)+' kg':'Checkpoint'}</b><div class="mut">${x.waist_cm!=null?'cintura '+num(x.waist_cm)+' cm':''}${x.arm_cm!=null?' • braço '+num(x.arm_cm)+' cm':''}${x.body_fat_pct!=null?' • gordura '+num(x.body_fat_pct)+'%':''}${x.notes?' • '+esc(x.notes):''}</div></div><span class="status">${x.energy?`energia ${x.energy}/5`:'salvo'}</span></div>`).join(''):'<div class="mut" style="padding:18px 0">Nenhum checkpoint ainda.</div>';$('cpForm').elements.measured_at.value=$('cpForm').elements.measured_at.value||iso()}

